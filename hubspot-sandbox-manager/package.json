{
  "name": "hubspot-sandbox-manager",
  "version": "1.0.0",
  "description": "HubSpot Sandbox Manager — static web app + Cloudflare Worker proxy",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy"
  }
}
